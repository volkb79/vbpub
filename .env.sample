# Shared vbpub environment (repo root)
# Copy to .env and fill in the values once for all projects.

# GitHub token for releases and GHCR (write:packages, read:packages)
# GITHUB_PUSH_PAT=ghp_your_token_here
# GITHUB_USERNAME=your-github-username-or-org
# GITHUB_REPO=vbpub
# CIU_DEBUG_API=1


# Optional registry overrides
# REGISTRY=ghcr.io

# Optional CIU latest asset defaults (if using release asset installs)
# CIU_LATEST_TAG=ciu-latest
# CIU_LATEST_ASSET_NAME=ciu-latest-py3-none-any.whl
