# Optional registry overrides
#
# Prefer placing shared credentials and settings in vbpub/.env
# (repo root). vsc-devcontainer scripts will read that file first.
# REGISTRY=ghcr.io
# NAMESPACE=your-github-username-or-org
# IMAGE_NAME=vsc-devcontainer

# Optional build metadata override
# BUILD_DATE=YYYYMMDD

# Optional backports mirror (deb822 URIs)
# BACKPORTS_URI=http://debian.anexia.at/debian

# GHCR authentication (personal account)
# GITHUB_GHCR_IO_USERNAME=your-github-username
# GITHUB_GHCR_IO_PAT=ghp_your_token_here

# Optional CIU wheel installation (derived URL)
# CIU_RELEASE_REPO=volkb79-2/vbpub
# CIU_RELEASE_TAG=ciu-latest
# CIU_WHEEL_FILENAME=ciu-latest-py3-none-any.whl
# CIU_WHEEL_SHA256=sha256hexvalue
#
# Optional override (explicit URL instead of derived values)
# CIU_WHEEL_URL=https://github.com/volkb79-2/vbpub/releases/download/ciu-latest/ciu-latest-py3-none-any.whl
